import SearchBar from "./Search";

const Header = () => {
  return (
    <div>
      <div className="header-text">Notes Application</div>
      <SearchBar />
    </div>
  );
};

export default Header;
